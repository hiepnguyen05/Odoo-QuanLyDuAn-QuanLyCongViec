<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Form View: Mở rộng Form Công việc -->
    <record id="view_task_form_inherit" model="ir.ui.view">
        <field name="name">project.task.form.inherit.quan_ly_du_an</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='allow_task_dependencies']" position="after">
                <field name="company_id" invisible="1"/>
                <field name="project_id" invisible="1"/>
                <field name="parent_id" invisible="1"/>
            </xpath>
            
            <!-- Ẩn user_ids mặc định để module quan_ly_cong_viec chèn nhan_vien_ids vào vị trí chuẩn -->
            <xpath expr="//field[@name='user_ids']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            

            
        </field>
    </record>
    
    <!-- Search View: Mở rộng Search View Công việc -->
    <record id="view_task_search_inherit" model="ir.ui.view">
        <field name="name">project.task.search.inherit.quan_ly_du_an</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_search_form"/>
        <field name="arch" type="xml">
            
            <!-- Thêm filter "Công việc của tôi" -->
            <xpath expr="//filter[@name='my_tasks']" position="after">
                <separator/>
                <filter string="Việc của tôi" 
                        name="my_tasks_custom" 
                        domain="[('user_ids', 'in', [uid])]"
                        help="Hiển thị tất cả công việc được gán cho tôi"/>
            </xpath>
            
        </field>
    </record>
    
    <!-- Tree View: Mở rộng Tree View Công việc -->
    <record id="view_task_tree_inherit" model="ir.ui.view">
        <field name="name">project.task.tree.inherit.quan_ly_du_an</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_tree2"/>
        <field name="arch" type="xml">
            
            <!-- Thêm màu sắc cho dòng -->
            <xpath expr="//tree" position="attributes">
                <attribute name="decoration-success">is_closed == True</attribute>
                <attribute name="decoration-danger">date_deadline and date_deadline &lt; current_date and is_closed == False</attribute>
                <attribute name="decoration-warning">date_deadline and date_deadline == current_date and is_closed == False</attribute>
            </xpath>
            
            <!-- Đổi cột Người thực hiện thành Nhân viên -->
            <xpath expr="//field[@name='user_ids']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='user_ids']" position="after">
                <field name="nhan_vien_ids" widget="many2many_tags"/>
            </xpath>
            
        </field>
    </record>

    <!-- Contextual Action: Gợi ý AI từ Action dropdown của Task -->
    <record id="action_generate_ai_from_task_menu" model="ir.actions.server">
        <field name="name">Nhờ AI Gợi Ý Cột &amp; Công Việc</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="binding_model_id" ref="project.model_project_task"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records[0].action_generate_tasks_ai_from_task()
        </field>
    </record>

</odoo>
